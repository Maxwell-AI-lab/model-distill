# 数据生成验证报告

> 生成时间: 2026-08-08 10:16:48
> Teacher: GLM-5.2 (智谱 BigModel, Anthropic 兼容 API)
> Student: Qwen3-8B

## 总览

| 指标 | 值 |
|------|------|
| 题目总数 | 50 |
| 训练集 | 40 题 |
| 评估集 | 10 题 |
| Teacher 生成 | 40 条 |
| 有效训练数据 | 33 条 |
| 有效率 | 40/40 (100.0%) |
| 有解题计划 | 40/40 (100.0%) |
| 有代码实现 | 40/40 (100.0%) |
| 有边界分析 | 40/40 (100.0%) |
| 平均计划步骤 | 3.6 步 |
| 步骤范围 | 3~6 步 |
| 平均代码长度 | 650 字符 |

## 数据样例 (2条)

### 样例 1: mbpp_771

**题目**: Write a function to check if the given expression is balanced or not....

**解题计划**:
```
1. **数据结构选择**：使用栈来解决括号匹配问题。栈具有“后进先出”（LIFO）的特性，非常适合处理这种嵌套结构的匹配。
2. **构建映射关系**：创建一个字典，将所有的右括号（`]`, `}`, `)`）映射到它们对应的左括号（`[`, `{`, `(`）。
3. **遍历字符串**：逐个字符检查给定的表达式字符串。
4. **处理左括号**：如果当前字符是左括号，直接将其压入栈中。
5. **处理右括号**：如果当前字符是右括号，检查栈是否为空。如果栈为空，说明没有对应的左括号，返回 `False`；如果栈不为空，弹出栈顶元素并与当前右括号的匹配值进行比较，若不匹配则返回 `Fals...
```

**代码**:
```python
def check_expression(expression: str) -> bool:
    """
    检查给定表达式字符串中的括号是否完全平衡且正确嵌套。
    """
    stack = []
    # 映射：右括号 -> 对应的左括号
    bracket_map = {')': '(', '}': '{', ']': '['}
    
    for char i...
```

### 样例 2: mbpp_694

**题目**: Write a function to extract unique values from the given dictionary values....

**解题计划**:
```
1. **提取元素**：遍历字典中所有的键值对，将所有的列表（或其他可迭代对象）合并或逐个提取出其中的元素。
2. **去重**：利用集合（Set）的互异性特性，将提取出的所有元素存入集合中，自动过滤掉重复的值。
3. **排序**：将去重后的集合转换回列表，并使用内置的排序函数（`sorted()`）对其进行升序排序，以满足测试用例中期望的输出格式。...
```

**代码**:
```python
def extract_unique(input_dict):
    """
    从字典的值（列表形式）中提取唯一元素，并返回排序后的列表。
    """
    # 使用集合推导式提取所有元素并去重
    unique_elements = {item for sublist in input_dict.values() for item in sublist}
    
    # ...
```


## 产出文件

| 文件 | 行数 | 大小 |
|------|------|------|
| data/tasks_train.jsonl | 40 | 31.3KB |
| data/tasks_eval.jsonl | 10 | 6.5KB |
| data/distill_raw.jsonl | 40 | 210.5KB |
| data/train_chatml.jsonl | 33 | 120.4KB |

## 下一步

- [ ] NPU 训练 (Qwen3-8B + LoRA)
- [ ] 评估 pass@1
- [ ] 分析结果，决定是否扩大到 500 题
